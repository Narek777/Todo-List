import {EventEmitter} from "fbemitter";
/**
 * adding new package for our Application
 * Creates a new Person
 * @constant
 * @class
 * @default
 */
const emitter = new EventEmitter();

/**
 * A module that exports variaable!
 * @exports emitter
 */
export {emitter};